package org.fiware.kong.pep.model.vc;

public class CredentialHolder {

    public VerifiableCredential credential;

    public CredentialHolder(VerifiableCredential credential) {
        this.credential = credential;
    }
}
